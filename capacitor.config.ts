import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bilibilianalyzer.app',
  appName: 'Bilibili Analyzer',
  webDir: 'out'
};

export default config;
